[workspace]
resolver = "2"
members = ["rome-core", "rome-cli", "rome-gui"]

# CLI-only builds (cargo install --git ... -p rome, used by install.sh) never
# touch rome-gui or its GUI dependencies at all -- that's the whole reason
# this is a workspace of 3 packages instead of one crate with 2 [[bin]]s.
